# Production dependencies for Render deployment (server only)
# Pinned versions for reproducible deploys on Python 3.11.

flask==3.1.3
flask-socketio==5.6.1
numpy==2.2.6
onnxruntime==1.20.1
python-dotenv==1.1.0

# gunicorn 25.x still ships the eventlet worker (removed in 26.x).
# eventlet is required when Render start command uses --worker-class eventlet.
gunicorn==25.2.0
eventlet==0.40.3

# gevent stack for Dockerfile / gunicorn_conf.py start command.
gevent==24.11.1
gevent-websocket==0.10.1
